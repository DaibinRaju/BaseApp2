<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.baseapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FamilyActivity" android:theme="@style/Activity"></activity>
        <activity android:name=".ColorsActivity" android:theme="@style/Activity" />
        <activity android:name=".PhrasesActvity" android:theme="@style/Activity" />
        <activity android:name=".NumbersActivity" android:theme="@style/Activity" />
        <activity android:name=".MiwokActivity" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

    </application>

</manifest>